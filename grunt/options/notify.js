'use strict';

module.exports = {
  build: {
    options: {
      title: 'Build complete',
      message: '<%= package.name %> build finished successfully.'
    }
  },
  generateReports: {
    options: {
      title: 'Task generateReports complete',
      message: '<%= package.name %> generateReports finished successfully.'
    }
  }
};
